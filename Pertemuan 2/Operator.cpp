#include <iostream>
using namespace std;

int main() {
    int a, b;

    system("CLS");
     a = 3;
     b = 5;

    // int tambah = a + b;
    // int kurang = a - b;
    // int kali = a * b;
    // // float bagi = a / b;         
    // float bagi = (float)a / b; //type casting                                                          
    // int modulo = a % b;

    // cout << "Hasil Penjumlahan: " << tambah << endl;
    // cout << "Hasil Pengurangan: " << kurang << endl;
    // cout << "Hasil Perkalian: " << kali << endl;
    // cout << "Hasil Pembagian: " << bagi << endl;
    // cout << "Hasil Modulo: " << modulo << endl;

    //realational Operator, membandingkan nilai dari dau operand.

    // cout << (a == b) << endl;
    // cout << (a < b) << endl;
    // cout << (a > b) << endl;
    // cout << (a != b) << endl;

    //logika operator (&&, ||, !), membandingkan 2 nilai variablel yg bertipe boolean

    // cout << (true && true) << endl; //true
    // cout << (true && false) << endl; //false

    // cout << (true || false) << endl; //true
    // cout << (false || false) << endl; //false

    // cout << !false << endl; //true
    // cout << !true << endl; //false


    // Bitwise Operator (&, |, ^, ~, <<, >>), operator bit
    // cout << (5 & 7) << endl; 
    // cout << (5 | 7) << endl;
    // cout << (5 ^ 7) << endl;
    // cout << (~7) << endl;
    // cout << (7<<2) << endl;
    // cout << (7>>2) << endl;

    // Operator Kondisional (?), operator ternary
    // int max`;
    // max = (a > b) ? a : b; //jika a lebih besar dari b, maka max = a, jika tidak maka max = b 
    // cout << "Nilai max: " << max << endl;

    // Short Hand Operator (+=, -=, *=, /=, %=), operator penugasan singkat
    // a += 7; // a = a + 7;
    // cout << "Nilai a: " << a << endl;

    //increment dan decrement operator (++, --), operator penambahan dan pengurangan 1
    //Pre Increment,  ++ ada di depan variabel
    // cout << a << endl;
    // cout << ++a << endl; //a = a + 1, baru ditampilkan = 4

    // Post Increment, ++ ada di belakang variabel
    // cout << a << endl; 
    // cout << a++ << endl; //a ditampilkan dulu baru ditambah 1 = 3
    // cout << a << endl; //a sudah ditambah 1 = 4

    // Pre Decrement, -- ada di depan variabel
    // cout << b << endl;
    // cout << --b << endl; //b = b - 1, baru ditampilkan = 4

    // Post Decrement, -- ada di belakang variabel
    // cout << b << endl;   
    // cout << b-- << endl; //b ditampilkan dulu baru dikurang 1 = 5
    // cout << b << endl; //b sudah dikurang 1 = 4
}